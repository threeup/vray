-- VRay Game Configuration (Lua)
-- All settings are optional; missing values use hardcoded defaults.

window = {
    width = 800,
    height = 600,
    fps = 60,
    fullscreen = false
}

camera = {
    pitch = 35.0,
    yaw = 23.0,
    roll = 0.0,
    fovy = 45.0,
    distance = 22.0
}

input = {
    move_speed = 15.0,
    rotation_speed = 2.5,
    zoom_speed = 3.0,
    zoom_min = 5.0,
    zoom_max = 80.0
}
